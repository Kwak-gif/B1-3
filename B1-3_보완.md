B1-3 노코드 자동화 설계: 워크플로우 설계

# 워크플로우 수행 계획(클로드_AI 활용)

🎯 최종 제출 결과물
📦 프로젝트 1: 자동화 도구 비교 구현
공통 워크플로우: Gmail 수신 → 조건 분기(제목 "긴급" 포함 여부) → [긴급] Discord 알림 + Sheets 기록 / [일반] Sheets 기록
도구 A: Make / 도구 B: n8n
💡 Zapier가 아닌 n8n인 이유: Zapier 무료 플랜은 2단계 Zap만 허용 → "Trigger + 분기 + Action 2개" 구조가 사실상 유료. n8n은 무료로 전체 구조 구현 가능 (이 근거 자체가 보고서 점수!)

산출물: 도구별 캡처 4종씩 + 비교 분석 보고서(비교 항목 5개 이상)
📦 프로젝트 2: 자유 주제 자동화
주제: 구글폼 문의 접수 → 문의 유형 분기 → Discord 알림 + Sheets 접수 대장 기록
산출물: 반복 업무 정의 + 도구 선정 이유 + 설계 문서(흐름도 포함) + 구현/실행 캡처 + 자동 실행 증빙
📂 PHASE 0. VS Code 폴더 세팅 (5분)
VS Code → File → Open Folder → 새 폴더 nocode-automation 생성 후 아래 구조로:

code
📋 복사
nocode-automation/
├── README.md
├── project1-comparison/
│   ├── make/
│   │   ├── blueprint.json
│   │   └── screenshots/
│   ├── n8n/
│   │   ├── workflow.json
│   │   └── screenshots/
│   └── comparison-report.md
└── project2-custom/
    ├── design.md
    ├── blueprint.json
    └── screenshots/
⚠️ 철칙: 캡처는 찍는 즉시 해당 폴더에 저장! 몰아서 하면 반드시 빠뜨립니다.

# 🔧 PHASE 1. Make로 워크플로우 구현
1-1. 사전 준비 (10분)
Google Sheets: 새 스프레드시트 이메일 기록 생성
시트 탭 2개: 긴급 / 일반
각 시트 1행에 헤더 입력: 수신시각 | 보낸사람 | 제목
Discord: 서버 아무거나 하나에서
채널 설정(⚙️) → 연동 → 웹후크 → 새 웹후크 → URL 복사해두기
Make 가입: make.com → 무료 플랜 가입
1-2. 시나리오 구성 (30분)
Make → Create a new scenario
Trigger: + 클릭 → Gmail 검색 → Watch Emails 선택
구글 계정 연결(권한 허용) → Folder: INBOX → Maximum results: 1
Router 추가: Gmail 모듈 우클릭 → Add a router (또는 모듈 검색에서 Flow Control → Router)
경로 A (긴급):
Router에서 나가는 선 클릭 → 필터(렌치 아이콘) 설정
Label: 긴급 / 조건: Subject → Contains → 긴급
다음 모듈: Discord → Send a Message(웹후크 방식) → 아까 복사한 웹후크 URL 붙여넣기
Message 예: 🚨 긴급 메일 도착! 제목: {{Subject}} / 보낸사람: {{From}}
이어서 모듈 추가: Google Sheets → Add a Row → 스프레드시트 이메일 기록 → 시트 긴급 → 각 열에 Date, From, Subject 매핑
경로 B (일반):
Router에서 두 번째 선 → 필터 설정
Label: 일반 / 조건: Subject → Does not contain → 긴급
모듈: Google Sheets → Add a Row → 시트 일반 → 동일 매핑
좌측 하단 Scheduling ON + 저장
1-3. 테스트 & 캡처 (15분)
자신의 Gmail로 테스트 메일 2통 발송:
① 제목: 긴급!! 코디세이 ② 제목: 주간 회의 안내
Make에서 Run once 클릭(또는 스케줄 대기) → 실행 확인
캡처 4종 → project1-comparison/make/screenshots/:
01_전체구성도.png (시나리오 전체 화면)
 ![alt text](image.png)
02_분기조건.png (필터 설정 화면)
 ![alt text](image-1.png)
 ![alt text](image-2.png)
03_경로A_실행결과.png (Discord 알림 + 긴급 시트 기록)
![alt text](image-3.png)
![alt text](image-4.png)
04_경로B_실행결과.png (일반 시트 기록)
![alt text](image-5.png)
Blueprint 내보내기: 시나리오 하단 ...(More) → Export Blueprint → make/blueprint.json으로 저장


# 🔧 PHASE 2. n8n으로 동일 워크플로우 구현
2-1. 준비
n8n Cloud 가입: cloud.n8n.io (무료 체험)
새 워크플로우 생성
2-2. 노드 구성 (Make와 1:1 대응)
code
📋 복사
Gmail Trigger (On message received)
   → IF 노드 (조건: Subject contains "긴급")
      ├─ true  → Discord 노드 → Google Sheets 노드 (긴급 시트)
      └─ false → Google Sheets 노드 (일반 시트)
Gmail Trigger 노드 추가 → 구글 계정 연결 → Event: Message Received
IF 노드 추가 → 조건: {{ $json.subject }} → String → Contains → 긴급
true 분기: Discord 노드(웹후크 URL 동일하게 사용) → Google Sheets 노드(Append Row, 긴급 시트)
false 분기: Google Sheets 노드(Append Row, 일반 시트)
워크플로우 Active 토글 ON
2-3. 테스트 & 캡처
테스트 메일 2통 재발송 (긴급 1 + 일반 1)
Executions 탭에서 각 경로 실행 로그 확인
캡처 4종 → n8n/screenshots/ (전체 구성도 / IF 조건 / 경로별 결과 2종)
 1) 전체 구성도![alt text](image-6.png)
 2) IF 조건 ![alt text](image-7.png)
 3) 결과_긴급 ![alt text](image-9.png)
 4) 결과_일반 ![alt text](image-10.png)

## 모니터링 지표 (Monitoring Metrics)

본 워크플로우의 각 노드(Gmail/Discord/Sheets)를 대상으로
Executions 탭 로그와 함께 다음 지표를 모니터링한다.

| 지표 | 측정 대상 | 임계값(알림 기준) | 대응 방안 |
|------|-----------|-------------------|-----------|
| Gmail 수신 성공률 | Trigger 폴링 성공 비율 | 95% 미만 시 알림 | 계정 인증·연결 상태 점검 |
| 분기 정확도 | "긴급" 조건 오분류 여부 | 오분류 1건이라도 발생 시 확인 | 필터 조건(Contains) 재검토 |
| Discord 전송 지연 | 알림 발송 소요 시간 | 평소 대비 2배 초과 시 알림 | 웹훅 429(요청 과다) 확인 |
| Sheets 기록 오류율 | Append Row 실패 비율 | 5분간 3회 이상 실패 시 알림 | API 쿼터 초과 여부 확인 |
| 재시도 발생률 | 노드 자동 재시도 비율 | 10% 초과 시 알림 | 근본 원인 분석(인증·쿼터) |

### 모니터링 운영 원칙
- Executions 탭(n8n) / History 탭(Make)에서 각 경로 로그를 주기 확인한다.
- 임계값 초과 시 담당자에게 자동 알림(Discord·이메일)을 발송한다.
- 지표는 재시도 정책·대체경로의 발동 기준과 연동한다.
  (예: Sheets 오류율 초과 → 재시도 → 3회 실패 시 대체경로 발동)

## 재시도 정책 (Retry Policy)

본 워크플로우의 각 노드(Gmail Trigger, Discord, Google Sheets)는
네트워크 오류·API 일시 장애에 대비해 다음 재시도 룰을 적용한다.

| 대상 노드 | 실패 원인 예시 | 재시도 룰 |
|-----------|----------------|-----------|
| Gmail Trigger | 폴링 중 인증 만료 | 최대 3회, 지수 백오프(1s→2s→4s) |
| Discord 알림 | 웹훅 429(요청 과다) | 최대 3회, 각 시도 간 5초 대기 |
| Google Sheets | API 쿼터 초과 | 최대 3회, 지수 백오프 + Jitter |

**적용 방법**
- n8n: 각 노드 Settings → `Retry On Fail` ON → Max Tries 3, Wait 지정
- Make: 시나리오 우클릭 → 오류 처리 → `Retry` 지시어 추가

**공통 원칙**
- Sheets 기록은 멱등성 확보를 위해 "수신시각+보낸사람"을 키로 중복 방지
- 인증 오류(4xx)는 재시도해도 실패하므로 즉시 알림 후 중단

## 대체경로 설계 (Fallback Path)

Discord 알림 노드가 최종 실패하더라도 데이터 유실이 없도록 설계한다.

Gmail 수신 → 분기(긴급 여부)
   ├─ [긴급] Discord 알림 시도
   │     ├─ 성공 → Sheets(긴급) 기록
   │     └─ 3회 실패 → Sheets(긴급)에 "알림실패" 플래그와 함께 기록
   │                  → 관리자 이메일로 대체 통지
   └─ [일반] Sheets(일반) 기록

- 핵심: **알림(Discord)이 실패해도 기록(Sheets)은 반드시 남긴다**
- n8n의 `Error Trigger` 또는 Make의 `Error Handler` 경로로 구현

## 모듈화 및 재사용 설계 (Modularization)

두 프로젝트가 공유하는 구조를 분석해 재사용 가능한 모듈을 정의한다.

### 1) 공통 노드 묶음 (프로젝트 1·2 공통 발견)
| 모듈 | 구성 노드 | 재사용처 |
|------|-----------|----------|
| 분기 모듈 | 조건 분기(Make: Router / n8n: IF) | P1(긴급), P2(환불) 동일 패턴 |
| 알림 모듈 | Discord 웹훅 발송 | 긴급 메일·환불 문의 공통 |
| 기록 모듈 | Google Sheets Append Row | 모든 경로에서 공통 사용 |

→ 프로젝트 1과 2는 "수신 → 분기 → 알림 + 기록"이라는
   **동일 골격을 재사용**한 것이 핵심이다.

### 2) 단계 분리 (Pipeline)
수신(Trigger) → 분기(Branch) → 처리(Notify/Record)
- 각 단계를 독립 구성해 트리거만 교체하면 다른 업무에 재적용 가능
  (예: Gmail → Google Forms로 트리거만 바꿔 프로젝트 2 구현)

### 3) 도구별 모듈화 특성
- **Make**: Router+Filter를 하나의 분기 모듈로 시각적 그룹화
- **n8n**: 공통 로직을 **서브 워크플로우**로 분리해 호출 가능
  → "긴급 처리" 서브 워크플로우를 만들면 여러 워크플로우에서 재호출

### 재사용 가이드라인
- 2회 이상 반복되는 구조(분기·알림·기록)는 모듈로 추출한다.
- 트리거와 처리 로직을 분리해, 입력원이 바뀌어도 처리부는 재사용한다.


우측 상단 메뉴 → Download → n8n/workflow.json 저장
🎯 Make와 n8n의 차이점: 
 1) Make: Work Flow 직접 구성, 구현, 명령어 입력, 연동 등의 수행 → 즉 툴을 이용을 위한 학습 필요
 2) n8n: AI를 통한 Work Flow 구현으로 명령어, 연동 등을 몰라도 개념만 가지고 수행 가능 

# 📝 PHASE 3. 비교 분석 보고서 (comparison-report.md)
markdown
📋 복사
# 자동화 도구 비교: Make vs n8n

## 1. 공통 워크플로우

**Gmail 수신 → 조건 분기(제목 "긴급" 포함 여부) → 분기별 처리**

## 2. 도구별 구현 과정 요약

- **트리거**: 새 메일 수신 (Make: Watch Emails / n8n: Gmail Trigger, 1분 폴링)
- **분기 조건**: 제목(Subject)에 "긴급" 문자열 포함 여부
- **최종 액션**: Discord 웹훅 알림 + Google Sheets 행 추가

### Make 
- 메일 수신 → 조건 분기 → "긴급" 포함 → 디스코드 알림 → 구글 시트_긴급_시간/발신/제목 입력
- 메일 수신 → 조건 분기 → "긴급" 미포함 → 구글 시트_일반_시간/발신/제목 입력
- Gmail `Watch Emails` 트리거 → **Router**로 2개 경로 분기
- 각 경로에 **필터(렌치 아이콘)** 로 조건 설정 (긴급: Contains / 일반: Does not contain)
- Discord·Sheets 모듈의 각 필드에 `{{Subject}}`, `{{From}}` 등 **변수를 수동으로 하나씩 매핑**
- 원형 노드를 선으로 연결하는 시각적 방식

### n8n
- n8n : 메일 수신 → 조건 분기 → "긴급" 포함 → 디스코드 알림 → 구글 시트_긴급_시간/발신/제목 입력
- n8n : 메일 수신 → 조건 분기 → "긴급" 미포함 → 구글 시트_일반_시간/발신/제목 입력
- Gmail Trigger → **IF 노드**로 true/false 분기
- 조건: `{{ $json.Subject }}` contains "긴급"
- **Set 노드("행 구성")** 로 데이터 정리 후 → Google Sheets `autoMapInputData`(자동 매핑) 사용
- Discord는 **HTTP Request 노드**로 웹훅에 직접 JSON 전송
- AI 지원으로 필드 매핑을 자동 생성 → 검증만 수행

## 3. 비교 분석 (5개 이상 필수!)
| 항목 | Make | n8n |
|------|------|-----|
| **UI/UX** | 원형 노드, 시각적·직관적 | 사각 노드, 개발자 친화적 |
| **데이터 매핑** | 필드마다 변수 **수동 연결** | Set 노드 + **autoMap 자동 매핑** |
| **조건 분기 방식** | Router + Filter(경로별 조건) | IF 노드(true/false 2갈래) |
| **외부 연동 방식** | Discord 전용 모듈 제공 | HTTP Request로 **웹훅 직접 호출** |
| **무료 플랜 범위** | 1,000 Ops/월 제한 | 자가호스팅 시 무제한(AI 구독자 기준) |
| **실행 로그 확인** | History 탭 | Executions 탭 |

## 4. 각 도구의 장단점

### Make
- ✅ **장점**: 원형 노드로 흐름이 한눈에 보임, Discord 등 전용 모듈이 많아 초기 연동이 쉬움, 필드를 직접 연결해 **결과 예측이 정확**
- ⚠️ **단점**: 필드가 많으면 수동 매핑이 번거로움, 무료 Ops(월 1,000회) 제한

### n8n
- ✅ **장점**: **AI·자동 매핑**으로 빠른 구성, HTTP Request 등으로 확장성이 높음, 자가호스팅 시 실행량 무제한
- ⚠️ **단점**: 자동 매핑 결과를 **직접 검증해야 함**(오매핑 위험), HTTP·JSON 등 약간의 기술 이해 필요

## 5. 상황별 추천 

- **비개발자 팀 / 작업량이 간단한 경우, 수행 경험 향상** → **Make**
  - 모듈화 및 기능 제안의 시각화로 학습 부담이 적음
- **비개발자 팀 / 작업량이 많은 경우** → **n8n**
  - 기능 구현에 대한 학습 없이, 다중/다량 작업 수행 시 AI로 구현 가능
- **데이터 통제·확장성·실행량이 중요하면** → **n8n**
  - HTTP·코드 노드로 자유롭게 확장 가능하고 자가호스팅 시 비용 부담 없음, 다만 AI 토큰은 별도(본인은 유료 구독자)

### 💡 핵심 가치 차이
- Make는 "내가 직접 연결하는 조립식"
- n8n은 "AI가 매핑까지 대신하는 자동식"
- n8n의 자동 매핑은 "AI가 해준 결과를 내가 검증한다"
- 이 검증 습관이 실무 자동화에서 오류를 막는 핵심이다.

#### 노코드 한계와 코드 기반 확장 (Code-Based Extension)

노코드 자동 매핑은 편리하지만 **오매핑 위험**과 **복잡한 조건 처리의 한계**가 있다.
이를 코드 노드(n8n: Code / Make: Custom JS)로 보완하는 확장 방안을 제시한다.

##### 1) 한계 사례별 코드 해결책

| 노코드 한계 | 코드 기반 확장 아이디어 |
|-------------|--------------------------|
| 자동 매핑 오매핑 위험 | Code 노드로 필드 검증 로직 추가 (필수값 누락·타입 체크) |
| "긴급" 단순 문자열 매칭의 한계 | 정규식·키워드 배열로 다중 조건 판별 |
| 반복 로직 중복 | 공통 함수를 Code 노드로 모듈화 |

##### 2) 예시: 매핑 검증 Code 노드 (n8n)

// n8n Code 노드 - Sheets 기록 전 데이터 검증
const item = $input.first().json;

// 필수 필드 검증 (오매핑 방지)
if (!item.From || !item.Subject) {
  throw new Error('필수 필드 누락: 매핑 오류 의심');
}

// "긴급" 키워드를 정규식으로 유연하게 판별
const urgentPattern = /긴급|urgent|ASAP|즉시/i;
const isUrgent = urgentPattern.test(item.Subject);

return {
  json: {
    receivedAt: new Date().toISOString(),
    from: item.From,
    subject: item.Subject,
    priority: isUrgent ? '긴급' : '일반',  // 검증된 분류값
  }
};

- 위 코드는 **자동 매핑 결과를 코드로 재검증**해 오매핑을 사전 차단한다.
- 단순 "Contains 긴급"보다 다양한 표현(urgent, ASAP)을 잡아낸다.

##### 3) 단계별 실행 로드맵

| 단계 | 목표 | 작업 내용 |
|------|------|-----------|
| 1단계 (현재) | 노코드 기본 구현 | Make/n8n 시각적 워크플로우 완성 ✅ |
| 2단계 | 검증 로직 추가 | Code 노드로 필드 검증·분류 정확도 향상 |
| 3단계 | 확장·연동 | HTTP Request로 외부 API 연동, DB 저장 |
| 4단계 | 완전 코드 전환 | 트래픽 증가 시 Python/Node.js 스크립트로 이관 |

##### 4) 확장 판단 기준
- 조건 분기가 3개 이상으로 복잡 → 코드 노드 전환
- 월 실행량이 노코드 무료 한도 초과 → 자가호스팅 + 코드화
- 외부 시스템 연동이 늘어남 → HTTP/API 코드 노드 활용

# 🔧 PHASE 4. 프로젝트 2: 자유 주제 구현
4-1. 설계 문서 먼저 작성 (design.md) — 순서가 평가 포인트!
markdown
📋 복사
# 자유 주제: 문의 접수 자동 분류 시스템

## 1. 반복 업무 정의
구글폼으로 들어온 문의를 사람이 확인해 유형별로 분류하고
담당 채널에 전달하는 작업 (건당 3분 × 일 10건 = 30분/일)

## 2. 도구 선정 및 이유
Make 선정 — 프로젝트 1 비교 결과 시각적 Router 설계가
직관적이고 무료 Ops 범위 내 구현 가능하여 선택

## 3. 워크플로우 흐름도
구글폼 제출 → Trigger(Watch Responses)
 → Router(문의 유형)
   ├─ [환불] → Discord 알림 → Sheets 기록
   └─ [일반] → Sheets 기록
4-2. 구현 (30분)
구글폼 생성: 질문 2개 — 문의 유형(객관식: 환불/일반) + 문의 내용(단답)
https://forms.gle/pT6JQKYNSKc7LkvN8
응답 탭 → 스프레드시트에 연결
Make 새 시나리오:
Trigger: Google Sheets → Watch New Rows (폼 응답 시트 연결)
Router 추가 → 경로 A 필터: 문의 유형 포함 환불 → Discord 알림 + Sheets(환불접수) 기록
경로 B 필터: 문의 유형 포함 일반 → Sheets(일반접수) 기록
Scheduling ON 필수! (자동 실행이 원문 제약사항)
4-3. 자동 실행 증빙 & 캡처
스케줄 ON 상태에서 폼 제출(환불 1건 + 일반 1건) → 손대지 않고 자동 실행되는지 확인 완료
캡처 → project2-고객문의자동화/screenshots/:
![alt text](image-11.png)
![alt text](image-12.png)
![alt text](image-13.png)
![alt text](image-14.png)
전체 구성도 / 분기 조건 / 경로별 실행 결과 2종 / History의 자동 실행 로그(Run once가 아닌 스케줄 실행 증거)
Blueprint 내보내기 → project2-고객문의자동화/blueprint.json

